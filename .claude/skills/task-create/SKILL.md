---
name: task-create
description: 新規タスクを作成する（issues.yaml + tasks/{id}.md + index.yaml）
argument-hint: "[タスクID タイトル]"
---

# タスク作成スキル

新規タスクを3ファイルに登録する。
引数: `/task-create 1-A-01-4 スキル棚卸しの更新` のように渡す。

## 引数の解析方法
- `$ARGUMENTS` 全体を空白で分割する
- **最初の単語** = タスクID（例: `1-A-01-4`）
- **残りすべて** = タスクタイトル（例: `スキル棚卸しの更新`）
- どちらかが欠けている場合はユーザーに入力を求めて終了する

## 手順

1. **引数のバリデーション**
   - タスクIDが命名規則 `{数字}-{英字}-{数字2桁}` または `{数字}-{英字}-{数字2桁}-{数字}` に従っているか確認する
   - タイトルが空でないことを確認する

2. **配置場所の確認**
   - `github_sim3/issues.yaml` を読み込む
   - タスクIDの階層構造から、追加先の正しい位置を特定する
     - 例: `1-A-01-4` → 戦略1 > カテゴリA > Issue01 の tasks リスト配下
   - 同じIDが既に存在しないことを確認する（存在する場合はエラー終了）

3. **issues.yaml にタスク追加**
   - 該当する親Issue/カテゴリ配下の `tasks:` リストに追加する:
     ```yaml
     - id: "{タスクID}"
       title: "{タイトル}"
       status: "open"
     ```
   - **インデントに細心の注意を払う**（スペース2つベース、周囲の既存エントリと揃える）

4. **tasks/{id}.md を作成**
   - `github_sim3/tasks/{タスクID}.md` を以下の雛形で作成する:
     ```markdown
     ---
     successCriteria:
       - "TODO: 完了条件を記入"
     outputs: []
     ---

     ## 詳細説明

     {タイトル}

     ## メモ

     ### YYYY-MM-DD 作成
     - /task-create で新規作成
     ```

5. **tasks/index.yaml にファイル名を追加**
   - `github_sim3/tasks/index.yaml` の `files:` リストに `"{タスクID}"` を追加する
   - コメントで区切られたセクション（Strategy区分）の正しい場所に挿入する
   - 例: `1-C-03-2` なら `# Strategy 1-C` セクション内の適切な位置

6. **結果報告**
   - 更新した3ファイルの変更箇所をユーザーに報告する
   - 「`successCriteria` を具体的に記入してください」と促す
   - Git操作は行わない（ユーザーが `/commit-push` で別途実施）
